{% extends 'base.html.twig' %}

{% block title %}User register{% endblock %}

{% block body %}
    {% if (errors is defined) %}
        {% for error in errors %}
            <span>{{ error }}</span>
        {% endfor %}
    {% endif %}
    {{ form(form) }}
{% endblock %}